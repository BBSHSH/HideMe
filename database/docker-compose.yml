version: "3.9"

services:
  db:
    image: postgres:16
    container_name: tsnet-postgres
    environment:
      POSTGRES_USER: tsnet
      POSTGRES_PASSWORD: tsnet
      POSTGRES_DB: tsnet_chat
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
